#pragma once
#include <string>
#include "json.hpp"

//Eventually message types here, with serializers

/*
struct Inputs {
	bool up{};
	bool down{};
	bool forwards{};
	bool back{};
	bool A{};
	bool B{};
	bool C{};
	bool D{};
};

NLOHMANN_DEFINE_TYPE_NON_INTRUSIVE(Inputs, up, down, forwards, back, A, B, C, D)*/